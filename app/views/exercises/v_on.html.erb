<!DOCTYPE html>
<html>
  <head>
    <title>Light Switch</title>
    <style>
      #app {
        border: dashed black 1px;
        display: inline-block;
        padding-bottom: 10px;
      }

      #app > button {
        display: block;
        margin: auto;
      }

      #lightDiv {
        position: relative;
        width: 150px;
        height: 150px;
      }

      #lightDiv > img {
        position: relative;
        width: 100%;
        height: 100%;
      }

      #lightDiv > div {
        position: absolute;
        top: 10%;
        left: 10%;
        width: 80%;
        height: 80%;
        border-radius: 50%;
        background-color: yellow;
      }


          #app3 {
      border: dashed black 1px;
      width: 350px;
      padding: 0 20px;
          }
          #app3 > div {
      width: 200px;
      height: 80px;
          }
          #app3 span {
      font-weight: bold;
      font-family: 'Courier New', Courier, monospace;
          }
    </style>
  </head>
  <body>
    <h1>Example: Light Switch</h1>
    <p>The v-on directive is used on the button tag to listen to the 'click' event. When the 'click' event occurs the 'lightOn' data property is toggled between 'true' and 'false', making the yellow div behind the lightbulb visible/hidden.</p>
    <div id="app">
      <div id="lightDiv">
        <div v-show="lightOn"></div>
        <img src="https://www.w3schools.com/vue/img_lightBulb.svg">
      </div>
      <button v-on:click=" lightOn =! lightOn ">Switch light</button>
    </div>
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script>
      const app = Vue.createApp({
        data() {
          return {
            lightOn: false
          }
        }
      })
       app.mount('#app')
    </script>
    <%# Second exercise from here %>
    <div id="app2" style="margin-top: 24px;">
      <input type="text" v-on:input="inpCount++" placeholder="Type something">
      <p>{{ 'Input events occured: '+inpCount }}</p>
    </div>
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script>
      const app2 = Vue.createApp({
        data() {
          return {
            inpCount: 0
          }
        }
      })
       app2.mount('#app2')
    </script>
    <%# Third app from here %>
    <h1>Example: Change Color</h1>
    <div id="app3">
      <p>Move the mouse pointer over the box below to change the background-color randomly with hsl color code.</p>
      <p> <strong>Caution</strong>: <u>The rapid color change may be too unsafe to those with certain conditions.</u></p>
      <div v-on:mousemove=" colorVal = Math.floor(Math.random()*360) " v-bind:style=" {backgroundColor: 'hsl('+colorVal+',60%,60%)'} "></div>
      <p><span>backgroundColor: hsl(<strong>{{ colorVal }}</strong>, 80%, 80%)</span></p>
    </div>
    <script src="https://unpkg.com/vue@3/dist/vue.global.js"></script>
    <script>
      const app3 = Vue.createApp({
        data() {
          return {
            colorVal: 50
          }
        }
      })
       app3.mount('#app3')
    </script>
  </body>
</html>
